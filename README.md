# k8s-cm-java-properties

Open the kubernetes at localhost:30007/actuator/dev



search for the word: `spring.application.name` 

it should have the value that is applied to the statefulset environment variable